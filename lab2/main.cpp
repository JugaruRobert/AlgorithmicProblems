#include <iostream>
#include "modul.h"
using namespace std;

int main()
{
    citire(a,n);
    cout<<"The smallest element is: "<<minim(a,n)<<endl;
    cout<<"The sum of prime elements is: "<<suma(a,n)<<endl;
    cout<<"The sum of all perfect square elements is: "<<sumapp(a,n)<<endl;
    cout<<"The product of all perfect square elements is: "<<prodpp(a,n)<<endl;
    cout<<"The sum of all prime elements is: " << primeSum(a,n)<<endl;
    return 0;
}
